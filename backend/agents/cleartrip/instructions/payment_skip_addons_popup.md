This is a POPUP-ONLY ACTION task.

If a confirmation popup is visible after skipping add-ons, click `Skip add ons` or the equivalent skip confirmation.

Rules:
- Target the popup only.
- Do NOT click `Okay, let me select` or any option that returns to add-ons.
- Do NOT scroll the page.
- Do NOT click any `Continue`, `Continue to payment`, `Pay`, or other non-popup button in this step.
- If the page is already showing contact details, traveller details, or the final payment page, stop immediately with no action.
- If no such popup is visible, do nothing and stop immediately.
- Stop as soon as the popup is dismissed or if no popup is present.
