You are on the Ixigo flight search page. Do the following steps in order:

1. **Source (From):** Select or enter the origin city. Use "{{from_city}}" or airport code "{{from_code}}" so the source is set to this city.

2. **Destination (To):** Select or enter the destination city. Use "{{to_city}}" or airport code "{{to_code}}" so the destination is set to this city.

3. **Trip type:** Select **One-way** (single journey). If One-way is already selected, leave it.

4. **Date:** Set the departure date to {{date}} (YYYY-MM-DD). Use the date picker or input if visible.

5. **Search:** Click the Search button (or "Search Flights") to run the search.

Complete the steps in order. After clicking Search, wait for the search to start (the page may navigate to results). Do not return any data; just perform the actions.
