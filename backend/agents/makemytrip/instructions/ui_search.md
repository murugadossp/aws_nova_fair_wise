Perform a flight search for flights from "{{from_city}}" to "{{to_city}}" on date "{{date}}" (YYYY-MM-DD).
You are currently on the MakeMyTrip homepage. Do the following:
1. Ensure the "Flights" section is active (it is selected by default).
2. Click the "From" input box, type the departure city ("{{from_city}}"), wait for the suggestions dropdown to appear, and select the correct airport.
3. Click the "To" input box, type the destination city ("{{to_city}}"), wait for the suggestions dropdown, and select the correct airport.
4. Click the "Departure" date section. Navigate the calendar to the target date ("{{date}}") and click it. 
5. Click the large "Search" button to perform the search.
6. Stop as soon as you have clicked "Search" and the next page begins to load. You do NOT need to wait for all the results to fully render.
