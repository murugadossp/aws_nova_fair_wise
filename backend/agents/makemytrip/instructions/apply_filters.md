You are on MakeMyTrip's flight search results page.
Apply the time-of-day filters below by clicking the appropriate buttons.

## Filters to apply

- **Departure time slot:** {{departure_slot}}
- **Arrival time slot:** {{arrival_slot}}

## How to apply each filter

**Departure:**
- If `{{departure_slot}}` is `none` — skip this section entirely.
- Otherwise: find the **"Departure From"** filter section (left sidebar or horizontal
  filter bar at the top of results). It contains four time-slot buttons:
  `Before 6 AM` · `6 AM to 12 PM` · `12 PM to 6 PM` · `After 6 PM`
- Click the button whose label matches **"{{departure_slot}}"** exactly.
- Wait 2 seconds for the results list to update before continuing.

**Arrival:**
- If `{{arrival_slot}}` is `none` — skip this section entirely.
- Otherwise: find the **"Arrival at"** filter section (same panel, below Departure).
  It contains the same four time-slot buttons.
- Click the button whose label matches **"{{arrival_slot}}"** exactly.
- Wait 2 seconds for the results list to update before continuing.

## Rules

- Click ONLY the specified button(s). Do NOT toggle any other filter.
- Do NOT click a button if the value for that filter is `none`.
- Do NOT change the airline, stops, price, or any other filter.
- Do NOT interact with the search form at the top of the page.
- After the final click (and its 2-second wait), finish this step immediately.
